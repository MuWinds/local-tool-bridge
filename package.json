{
  "name": "local-tool-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Give ChatGPT / Codex the ability to call local tools, via an MCP endpoint bridged to a cross-platform Rust host.",
  "scripts": {
    "test": "cd apps/desktop && cargo test"
  }
}
